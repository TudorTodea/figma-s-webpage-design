import styled from "@emotion/styled";
import { Typography } from "@mui/material";

export const FooterTitle = styled(Typography)(() => ({


}))

export const FooterIconsTypography = styled(Typography)(() => ({
    display: 'flex',
    alignItems: 'center',
    marginTop: 10
}))